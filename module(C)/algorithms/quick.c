#include <stdio.h>
#include <stdlib.h>
int main(int argc, char const *argv[])
{
	int n,a[n],temp,pivot,high,low;
	
   	time_t t;
	printf("enter the size of an array:");
	scanf("%d",&n);
    srand((unsigned) time(&t));
 
 for(int i = 0 ; i < n ; i++ ) 
   {
     a[i]=rand()%50;
     printf("%d\n",a[i]);
   }
	printf("this is logic for quick sort\n");
	for (int i = 0; i < n-1; ++i)
	{
	while()
	}
	
	return 0;
}