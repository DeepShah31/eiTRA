#include <stdio.h>
#include <unistd.h>
#include <stdlib.h>
//#include <sys/wait.h>

int main(int argc, char *argv[])‏
{
	int pid1,pid2,pid3,pid4;

	pid1=fork();

	if (pid1>0)
	{
		pid2=fork();
	}
	if (pid2>0)
	{
		pid3=fork();
	}
	if (pid3>0)
	{
		pid4=fork();
	}
return 0; 
} // End main
